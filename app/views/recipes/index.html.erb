<h1>レシピ一覧</h1>
<% @recipes.each do |recipe| %>
    <div class="row">
      <%= image_tag recipe.image.url, class: "col-md-2" %>
      <h2><%= link_to recipe.title, recipe_path(recipe) %><br></h2>
      <hr>
      <p><%= recipe.ingredients %></p>
    </div>
<% end %>